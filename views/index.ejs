<!DOCTYPE html>
<html lang="en">
    <%- include('layouts/header'); -%>
    <body id="page-top">
        <nav class="navbar navbar-expand-lg bg-success text-uppercase fixed-top" id="mainNav">
            <div class="container">
                <a class="navbar-brand" href="#page-top">Nutrition & Activity</a>
                <button class="navbar-toggler text-uppercase font-weight-bold bg-primary text-white rounded" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                    Menu
                    <i class="fas fa-bars"></i>
                </button>
                <div class="collapse navbar-collapse" id="navbarResponsive">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item mx-0 mx-lg-1"><a class="nav-link py-3 px-0 px-lg-3 rounded" href="/"><%= language === 'en' ? 'Main Page' : 'Главная страница' %></a></li>
                        <li class="nav-item mx-0 mx-lg-1"><a class="nav-link py-3 px-0 px-lg-3 rounded" href="/calories/en"><%= language === 'en' ? 'Calories burned' : 'Сжигание калорий' %></a></li>
                        <li class="nav-item mx-0 mx-lg-1"><a class="nav-link py-3 px-0 px-lg-3 rounded" href="/nutrition/en"><%= language === 'en' ? 'Nutrition' : 'Питание' %></a></li>
                        <li class="nav-item mx-0 mx-lg-1"><a class="nav-link py-3 px-0 px-lg-3 rounded" href="/login"><%= language === 'en' ? 'Authorization' : 'Авторизация' %></a></li>
                        <li class="nav-item mx-0 mx-lg-1"><a class="nav-link py-3 px-0 px-lg-3 rounded" href="/posts"><%= language === 'en' ? 'For Admins' : 'Для админов' %></a></li>
                    </ul>
                </div>
            </div>
        </nav>

        <style>
            
            .post-container {
                background-color: #f8f9fa;
                border-radius: 10px;
                padding: 20px;
                margin-bottom: 30px;
                box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.1);
            }

            .post-title {
                font-size: 24px;
                color: #333;
                margin-bottom: 10px;
                text-align: center;
            }

            .post-description {
                font-size: 16px;
                color: #666;
                margin-bottom: 20px;
            }

            .carousel-img {
                max-height: 300px;
                width: auto;
                margin: 0 auto;
                display: block;
            }

            .carousel-control-prev-icon,
            .carousel-control-next-icon {
                background-color: #333;
                border-radius: 50%;
                padding: 10px;
            }

            .carousel-control-prev,
            .carousel-control-next {
                width: auto;
                padding: 15px;
            }
        </style>


        <div class="container" style="margin-top: 150px; margin-bottom: 50px;">
            <div class="row justify-content-end">
                <div class="col-auto">
                    <form action="/" method="GET">
                        <select name="language" id="language" >
                            <option value="en">English</option>
                            <option value="ru">Russian</option>
                        </select>
                        <button type="submit" ><%= language === 'en' ? 'Change language' : 'Поменять язык' %></button>
                    </form>
                </div>
            </div>
        </div>


        <h1 class="display-4" style="text-align: center;"> <%= language === 'en' ? 'Posts' : 'Посты' %></h1>
        <section>
            <div class="container">
                <div class="row">
                    <% posts.forEach((post, index) => { %>
                        <div class="col-md-8 offset-md-2">
                            <div class="post-container mt-5">
                                <div id="carouselExampleControls<%= index %>" class="carousel slide" data-ride="carousel">
                                    <div class="carousel-inner">
                                        <div class="carousel-item active">
                                            <img src="<%= post.image1 %>" class="d-block mx-auto carousel-img" alt="Slide 1">
                                        </div>
                                        <div class="carousel-item">
                                            <img src="<%= post.image2 %>" class="d-block mx-auto carousel-img" alt="Slide 2">
                                        </div>
                                        <div class="carousel-item">
                                            <img src="<%= post.image3 %>" class="d-block mx-auto carousel-img" alt="Slide 3">
                                        </div>
                                    </div>
                                    
                                    <a class="carousel-control-prev" href="#carouselExampleControls<%= index %>" role="button" data-slide="prev">
                                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                        <span class="sr-only">Previous</span>
                                    </a>
                                    <a class="carousel-control-next" href="#carouselExampleControls<%= index %>" role="button" data-slide="next">
                                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                        <span class="sr-only">Next</span>
                                    </a>
                                </div>

                                <h2 class="post-title mt-3"><%= language === 'en' ? post.title_eng : post.title_ru %></h2>
                                <p style="text-align: center; color: #000;"><%= language === 'en' ? post.desc_eng : post.desc_ru %></p>
                                <p class="post-description" style="text-align: right;"><%= language === 'en' ? 'Posted' : 'Опубликовано' %>: <%= post.creationDate.toLocaleString('en-US', { timeZone: 'Asia/Almaty' }) %></p>
                                    
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
        </section>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

        <%- include('layouts/footer'); -%>
        <%- include('layouts/scripts'); -%>
    </body>
</html>
